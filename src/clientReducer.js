export default (previousState = [], action) => {
  switch (action.type) {
    // case 'CLIENTS' :
    //     return {
    //         clients: previousState.clients
    //     }
    default:
      return previousState;
  }
};
